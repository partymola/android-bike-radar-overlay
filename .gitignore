# Build / IDE artefacts
.gradle/
.gradle-cache/
build/
app/build/
local.properties
.idea/
*.iml
.kotlin/
.DS_Store
debug.keystore

# Kotlin-only repo; never track stray Java.
*.java

# Local-only working tree: research, drafts, ride captures, analysis,
# tooling output, and anything else that must never enter public
# history. One rule - nothing under do-not-commit/ is ever committed.
do-not-commit/
