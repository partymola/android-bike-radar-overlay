# Run all Robolectric tests against SDK 35 even though the app targets SDK 36.
# Robolectric 4.14's SDK 36 runtime has incomplete shadows (Typeface NPE,
# SystemProperties native-link error). Remove this override once those are fixed
# upstream.
sdk=35
